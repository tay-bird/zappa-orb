description: >
  Provision a machine executor.

parameters:
  version:
    type: "string"
    default: "2.7"
    description:
      The Python version to use in the executor.

resource_class: small

machine: true
